<?php use Doctrine\Common\Collections\ArrayCollection;
 /**
 * @Entity
 * @Table(name="jabaianb.voyage")
 */ class voyage{
        /** @Id @Column(type="integer")
         * @GeneratedValue
         */
        public $id;
        /** @Column(type="integer")
            @ManyToOne(targetEntity="utilisateur")
            @JoinColumn(name="conducteur",referencedColumnName="id")
 */
/**        public $conducteur;
            @ManyToOne(targetEntity="trajet")
            @JoinColumn(name="trajet",referencedColumnName="id")
 */
        public $trajet;
        /**@Column(type="integer") */
       public $tarif;
        /** @Column(type="integer") */
        public $nbplace;
 /** @Column(type="integer") */
        public $heuredepart;
         /** @Column(type="string", length=500) */
        public $contraintes; /**
     * voyage constructor.
     * @param $id
     * @param $conducteur
     * @param $trajet
     * @param $tarif
     * @param $nbplace
     * @param $heuredepart
     * @param $contraintes
     */
   function __construct($id,$conducteur, $trajet, $tarif, $nbplace, $heuredepart, $contraintes){
        $this->id = $id;
        $this->conducteur = $conducteur;
        $this->trajet = $trajet;
        $this->tarif = $tarif;
        $this->nbplace = $nbplace;
        $this->heuredepart = $heuredepart;
        $this->contraintes = $contraintes;
    }
}
?>
